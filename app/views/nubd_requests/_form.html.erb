<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Form Title</title>
  <style>
      /* CSS styles */
      body {
          margin: 0;
          padding: 0;
          font-family: Arial, sans-serif;
          background-color: #f0f8ff; /* Light blue background */
      }

      #nubd-form {
          max-width: 500px;
          margin: 50px auto;
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background for the form */
          animation: fadeInAnimation 1s ease-in-out; /* Apply animation */
      }

      @keyframes fadeInAnimation {
          0% {
              opacity: 0;
              transform: translateY(-20px);
          }
          100% {
              opacity: 1;
              transform: translateY(0);
          }
      }

      /* Additional styling for form elements */
      .my-5 {
          margin-bottom: 20px;
      }

      .shadow {
          box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .rounded-md {
          border-radius: 6px;
      }

      .border {
          border-width: 1px;
          border-style: solid;
      }

      .border-gray-200 {
          border-color: #edf2f7;
      }

      .outline-none {
          outline: none;
      }

      .px-3 {
          padding-left: 12px;
          padding-right: 12px;
      }

      .py-2 {
          padding-top: 8px;
          padding-bottom: 8px;
      }

      .w-full {
          width: 100%;
      }

      .bg-blue-600 {
          background-color: #2563eb;
      }

      .text-white {
          color: #fff;
      }

      .inline-block {
          display: inline-block;
      }

      .font-medium {
          font-weight: 500;
      }

      .cursor-pointer {
          cursor: pointer;
      }

      .rounded-lg {
          border-radius: 8px;
      }

      /* Hover animation */
      .hover\:scale-105:hover {
          transform: scale(1.05);
      }
  </style>
</head>
<body>
<!-- Form HTML -->
<div id="nubd-form">
  <%= form_with(model: nubd_request, class: "contents") do |form| %>
    <% if nubd_request.errors.any? %>
      <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
        <h2><%= pluralize(nubd_request.errors.count, "error") %> prohibited this nubd_request from being saved:</h2>

        <ul>
          <% nubd_request.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="my-5">
      <%= form.label :name %>
      <%= form.text_field :name, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :email %>
      <%= form.text_field :email, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :mobile %>
      <%= form.text_field :mobile, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :department_id %>
      <%= form.text_field :department_id, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :subject_id %>
      <%= form.text_field :subject_id, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :session_year %>
      <%= form.text_field :session_year, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :is_active %>
      <%= form.check_box :is_active, class: "block mt-2 h-5 w-5 transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="my-5">
      <%= form.label :txt_id %>
      <%= form.text_field :txt_id, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>

    <div class="inline">
      <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer transition duration-300 ease-in-out transform hover:scale-105" %>
    </div>
  <% end %>
</div>

</body>
</html>
