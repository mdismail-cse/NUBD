<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Form Title</title>
  <style>
      /* CSS styles */
      body {
          margin: 0;
          padding: 0;
          font-family: Arial, sans-serif;
          background-color: #f0f8ff; /* Light blue background */
      }

      #form-container {
          max-width: 500px;
          margin: 50px auto;
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      h1 {
          text-align: center;
          animation: fadeInAnimation 1s ease-in-out; /* Apply animation */
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Change font type */
          color: #fff; /* Text color */
      }

      @keyframes fadeInAnimation {
          0% {
              opacity: 0;
              transform: translateY(-20px);
          }
          100% {
              opacity: 1;
              transform: translateY(0);
          }
      }

      /* Additional styling for form elements */
      .bg-blue-600 {
          background-color: #2563eb;
      }

      .p-5 {
          padding: 20px;
      }

      .rounded-lg {
          border-radius: 8px;
      }

      /* Link styling */
      .link {
          display: inline-block;
          margin-top: 20px;
          padding: 10px 20px;
          background-color: #f0f0f0;
          border-radius: 8px;
          color: #333;
          text-decoration: none;
          font-weight: bold;
          transition: background-color 0.3s;
      }

      .link:hover {
          background-color: #ddd;
      }
  </style>
</head>
<body>
<!-- Form HTML -->
<div id="form-container" class="bg-blue-600 p-5 rounded-lg">
  <h1 class="font-bold text-4xl">NUBD Requests</h1>

  <%= render "form", nubd_request: @nubd_request %>
</div>

<a href="<%= nubd_requests_path %>" class="link">Back to nubd requests</a>

</body>
</html>
